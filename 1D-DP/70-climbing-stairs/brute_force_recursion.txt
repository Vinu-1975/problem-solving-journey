class Solution:
    def climbStairs(self, n: int) -> int:
        # brute-force recursion
        # time - O(2^n)
        # space - O(n)

        if n == 0 or n == 1:
            return 1
        return self.climbStairs(n-1) + self.climbStairs(n-2)