#User function Template for python3

class Solution:
    def rotate(self, arr):
        
        l = 0
        r = len(arr)-1
        
        while l!=r:
            arr[l],arr[r] = arr[r],arr[l]
            l+=1
        